# Replace the line below with your AWS secret access key only (one line, no spaces).
# Then save as: aws-secret-access-key.value
# Secret name in GSM must be: aws-secret-access-key

PUT_SECRET_ACCESS_KEY_HERE
