# Replace the line below with your AWS access key only (one line, no spaces).
# Then save as: aws-access-key-id.value  (or pipe directly to gcloud; see upload script.)
# Secret name in GSM must be: aws-access-key-id

PUT_ACCESS_KEY_ID_HERE
