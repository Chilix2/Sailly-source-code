"""Flow Builder backend — introspection API for visualizing and editing the conversation graph.

This module provides read-only graph introspection via `/api/builder/*` endpoints.
No changes are made to running brain logic; all routes operate on source files and config.
"""
