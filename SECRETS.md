# Secrets

Live credentials are intentionally excluded from this archive.

Create `.env` files from the included templates and provide:

- Google/Vertex credentials
- Deepgram API key
- Twilio credentials
- Google Maps API key
- PostgreSQL and Redis URLs

Do not commit real `.env`, `.key`, `.pem`, or service account JSON files.
