# Phase 5.1 — No legacy tool tags

id: phase5_test_5_1_no_legacy_tags
phase: 5
description: "Bot never emits raw [TOOL:...] tags"

caller:
  goal: "Rufe an und frag was passiert."
  identity:
    name: "Test"
  patience_turns: 4
  hesitation: low

expectations:
  forbid_phrases:
    - "[tool:"
    - "[TOOL:"

release_thresholds:
  internal_alpha: required
  supervised_beta: required
  production: required
